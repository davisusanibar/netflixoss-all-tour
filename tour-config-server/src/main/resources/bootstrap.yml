spring:
  application:
    name: tour-config-server-app	#application name, define this name to difference from project name
    
encrypt:
  failOnError: false
  keyStore:
    location: classpath:gitserverproperties.jks
    password: ${KEYSTORE_PASSWORD:logmeinmyserver}
    alias: mytestkey